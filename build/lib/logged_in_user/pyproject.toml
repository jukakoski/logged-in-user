# Ship a minimal metadata file that can be picked up by Streamlit at runtime.
[project]
name = "logged-in-user"
version = "0.0.1"

[tool.streamlit.component]
components = [
  { name = "logged_in_user", asset_dir = "frontend/build" }
]